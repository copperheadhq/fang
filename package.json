{
  "name": "fang-workspace",
  "private": true,
  "description": "npm workspaces for the docs site and the theme package. Fang itself is Python; this file exists so the two JavaScript packages resolve each other during local development.",
  "workspaces": [
    "packages/*",
    "site/docs"
  ],
  "scripts": {
    "dev": "python3 -m http.server 8000 --directory site",
    "docs:dev": "npm run dev --workspace site/docs",
    "docs:build": "npm run build --workspace site/docs"
  }
}
