// @ts-check
import { defineConfig } from "astro/config";
import starlight from "@astrojs/starlight";

// The whole of fang's identity is inherited: the theme file supplies the
// palette and the type, the brand assets supply the mark. Nothing below is a
// visual decision — those live in the theme, one level up in copperhead.
export default defineConfig({
  site: "https://fang.copperhead.sh",
  integrations: [
    starlight({
      title: "fang",
      description:
        "The language and kernel under copperhead. Write hardware in Python; " +
        "the kernel decides what is true about it.",
      logo: {
        src: "./public/brand/mark.svg",
        alt: "fang",
      },
      favicon: "/brand/favicon.svg",
      customCss: ["./src/styles/copperhead.css"],
      social: [
        { icon: "github", label: "GitHub", href: "https://github.com/copperheadhq/fang" },
      ],
      editLink: {
        baseUrl: "https://github.com/copperheadhq/fang/edit/main/site/docs/",
      },
      // Autogenerated from the directory tree, so adding a page means adding a
      // file and nothing else.
      sidebar: [
        { label: "Start here", items: [{ autogenerate: { directory: "start" } }] },
        { label: "Concepts", items: [{ autogenerate: { directory: "concepts" } }] },
        { label: "Reference", items: [{ autogenerate: { directory: "reference" } }] },
      ],
    }),
  ],
});
